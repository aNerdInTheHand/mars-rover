const moveRobot = ({
  C,
  robotIsLost
}) => ({
  boundaryX,
  boundaryY,
  commandList,
  currentOrientation,
  currentX,
  currentY
}) => {
  let position = {
    currentX,
    currentY,
    currentOrientation
  }
  return 'TODO'
}

module.exports = moveRobot